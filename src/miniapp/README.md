Mini apps are:

- A single React + Tailwind JSX file that exports a default component
- Use a fixed "harness" consisting of a static HTML file and default Tailwind config
- Compiled entirely in the browser with esbuild-wasm
